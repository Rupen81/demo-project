import "./button.css"

function Button({content}){
    return(
        <div className="Btn">
            {/* <button type="">{content ? content : "Click here"}</button> */}
            
        </div>
    );
}

export default Button;